using Microsoft.AspNetCore.Identity;

namespace LearnitySchool.Infrastructure.Identity;

public class ApplicationRole : IdentityRole
{
    // Поки порожня. Можеш потім додати поля (наприклад Description).
}
